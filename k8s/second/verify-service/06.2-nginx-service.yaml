apiVersion: v1
kind: Service
metadata:
  name: verify-service
  namespace: k8s-basics
  labels:
    app: verify-service-nginx-deploy
spec:
  type: NodePort
  selector:
    app: verify-service-nginx-deploy
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30089
