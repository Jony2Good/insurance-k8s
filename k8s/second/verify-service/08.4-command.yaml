apiVersion: apps/v1
kind: Deployment
metadata:
  name: verify-calc-event
  namespace: k8s-basics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: verify-calc-event
  template:
    metadata:
      labels:
        app: verify-calc-event
    spec:
      initContainers:
        - name: wait-for-php
          image: alpine:3.18
          command: ['sh', '-c', 'apk add --no-cache netcat-openbsd && until nc -z verify-php-service 9000; do echo "Waiting for verify-php-service..."; sleep 2; done;']
      containers:
        - name: verify-calc-event
          image: atrem2023/verify-service-v2:amd64
          command: ["php", "artisan", "app:calc-event"]      
          env:
          - name: RABBITMQ_HOST
            value: "rabbitmq"
          - name: RABBITMQ_PORT
            value: "5672"