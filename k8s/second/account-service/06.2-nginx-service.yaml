apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: k8s-basics
  labels:
    app: account-service-nginx-deploy
spec:
  type: NodePort
  selector:
    app: account-service-nginx-deploy
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30082
