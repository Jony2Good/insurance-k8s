apiVersion: apps/v1
kind: Deployment
metadata:
  name: calc-service-nginx-deploy
  namespace: k8s-basics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calc-service-nginx-deploy
  template:
    metadata:
      labels:
        app: calc-service-nginx-deploy
    spec:
      containers:
        - name: nginx
          image: atrem2023/app-nginx:amd64
          ports:
            - containerPort: 80
          volumeMounts:
            - name: calc-service-nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf          
      volumes:
        - name: calc-service-nginx-config
          configMap:
            name: calc-service-nginx-config
