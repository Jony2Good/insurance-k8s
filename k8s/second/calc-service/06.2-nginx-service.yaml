apiVersion: v1
kind: Service
metadata:
  name: calc-service
  namespace: k8s-basics
  labels:
    app: calc-service-nginx-deploy
spec:
  type: NodePort
  selector:
    app: calc-service-nginx-deploy
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30086
